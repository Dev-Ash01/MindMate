# Environment Configuration for Mental Wellness Chatbot

# ==================== LLM Provider Configuration ====================
# Choose between "huggingface-api" (requires API key) or "ollama" (local)

# Option 1: HuggingFace API (Free tier with rate limits)
LLM_PROVIDER=huggingface-api
# Get your free API key from: https://huggingface.co/settings/tokens
# This is required if using HuggingFace provider
HF_API_KEY=hf_your_api_key_here

# Option 2: Ollama (Local LLM - No API key needed)
# Uncomment this section if you want to use Ollama instead
# LLM_PROVIDER=ollama
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=mistral  # or: neural-chat, llama2, etc.

# ==================== Application Configuration ====================
# Server host and port
HOST=0.0.0.0
PORT=8000

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Environment: development, production
ENVIRONMENT=development

# ==================== Advanced Configuration ====================
# Sentiment model (do not change unless you know what you're doing)
# Alternative: "distilbert-base-uncased" or "bert-base-uncased"
SENTIMENT_MODEL=distilbert-base-uncased-finetuned-sst-2-english

# NLP device: -1 for CPU, 0 for GPU
NLP_DEVICE=-1

# Max conversation history to send to LLM (for context)
MAX_CONTEXT_LENGTH=2000

# Response generation timeout in seconds
RESPONSE_TIMEOUT=15
